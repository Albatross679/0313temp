---
phase: 03-metrics-and-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - report/report.tex
autonomous: false
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]

must_haves:
  truths:
    - "The LLM results table in report.tex lists actual experiment names (BM25 k=3, Random k=0, etc.) instead of placeholder Variant1/Variant2/Variant3"
    - "All dev metric cells in both T5 and LLM tables contain real numeric values, not XX.XX"
    - "Test metric cells contain either Gradescope scores or a clear TBD marker pending submission"
    - "Metric format is consistent: percentages with % in column headers, two decimal places"
  artifacts:
    - path: "report/report.tex"
      provides: "Filled results tables for T5 (Parts 1 & 2) and LLM (Part 3)"
      contains: "BM25"
  key_links:
    - from: "report/report.tex"
      to: "results/*_log.txt"
      via: "dev metrics extracted from log files and inserted into LaTeX table cells"
      pattern: "17\\.35|12\\.60|11\\.96"
---

<objective>
Fill all report results tables with actual dev metrics and restructure the LLM table with real experiment names. Handle test metric placeholders via Gradescope submission.

Purpose: The report tables currently have XX.XX placeholders and generic Variant1/Variant2 names. This plan replaces all placeholders with real metrics from existing experiment logs and restructures the LLM table to reflect actual experiments run.
Output: report.tex with all dev metrics filled and LLM table restructured. Test metrics either filled (if Gradescope scores available) or clearly marked TBD.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-metrics-and-visualization/03-RESEARCH.md

@report/report.tex
@experiments/llm-prompting-experiments.md

<interfaces>
<!-- Metric values from experiment logs to insert into report tables. -->

From experiment logs (dev set, already computed):

T5 Fine-tune dev (report.tex line 463, already filled):
  Query EM = 2.67%, Record F1 = 79.60%

T5 From-scratch dev (report.tex line 478, already filled):
  Query EM = 3.22%, Record F1 = 66.12%

LLM Prompting dev (from results/*_log.txt):
  BM25 k=3 (best):        SQL EM = 0.00%, Record F1 = 17.35%
  Random k=0 (zero-shot):  SQL EM = 0.00%, Record F1 = 12.60%
  Random k=1:              SQL EM = 0.00%, Record F1 = 12.60%
  Random k=3:              SQL EM = 0.00%, Record F1 = 11.96%
  No instructions (abl):   SQL EM = 0.00%, Record F1 = 11.80%
  No schema (abl):         SQL EM = 0.00%, Record F1 = 11.80%
  Schema only (abl):       SQL EM = 0.00%, Record F1 = 11.37%

T5 test metrics: Require Gradescope submission (no local ground truth).
LLM test metrics: Require Gradescope submission (no local ground truth).

Current report.tex placeholder locations:
  Line 482: T5 fine-tuning test -- XX.XX & XX.XX
  Line 483: T5 from scratch test -- XX.XX & XX.XX
  Lines 500-509: Entire LLM table has XX.XX placeholders and generic Variant names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill dev metrics and restructure LLM results table in report.tex</name>
  <files>report/report.tex</files>
  <action>
Edit report/report.tex to fill all dev metric placeholders and restructure the LLM results table.

**1a. Restructure the LLM results table (lines 491-514):**

Replace the entire LLM table (from `\begin{table}[h!]` through `\end{table}` containing `\label{tab:results_llm}`) with the following restructured version using actual experiment names and dev metrics:

```latex
\begin{table}[h!]
\centering
\begin{tabular}{lcc}
  \toprule
  System & Query EM (\%) & F1 (\%) \\
  \midrule
  \multicolumn{3}{l}{\textbf{Dev Results}} \\
  \midrule
  \multicolumn{3}{l}{\emph{k-shot prompting (random selection)}} \\
  Random $k=0$ (zero-shot)    & 0.00 & 12.60 \\
  Random $k=1$                & 0.00 & 12.60 \\
  Random $k=3$                & 0.00 & 11.96 \\[4pt]
  \multicolumn{3}{l}{\emph{BM25 per-query selection}} \\
  \textbf{BM25 $k=3$ (best)}  & 0.00 & \textbf{17.35} \\[4pt]
  \multicolumn{3}{l}{\emph{Ablation study (BM25 $k=3$)}} \\
  No instructions              & 0.00 & 11.80 \\
  No schema                    & 0.00 & 11.80 \\
  Schema only (no instr., no examples) & 0.00 & 11.37 \\
  \midrule
  \multicolumn{3}{l}{\textbf{Test Results}} \\
  \midrule
  BM25 $k=3$                  & XX.XX & XX.XX \\
  \bottomrule
\end{tabular}
\caption{LLM prompting development and test results (Part 3). Query EM and Record F1 on the development set for Gemma-2B with various $k$-shot and example selection strategies. Best dev result in bold. Ablations remove individual prompt components from the best (BM25 $k=3$) configuration.}
\label{tab:results_llm}
\end{table}
```

Key changes vs the template:
- Column headers now use "Query EM (\%)" and "F1 (\%)" to match the T5 table convention
- Replaced generic Variant1/2/3/4/5 with actual experiment names
- Grouped into k-shot random, BM25, and ablation sections with `\emph` subheadings
- Bold on best dev result (BM25 k=3, F1=17.35)
- Test row uses "BM25 $k=3$" (the submitted test config) with XX.XX pending Gradescope
- Caption updated to describe actual experiments

**1b. T5 test rows (lines 482-483):**

Leave the T5 test XX.XX placeholders as-is for now. These will be filled after Gradescope submission in Task 2.

The T5 dev metrics are already filled in the report (lines 463, 478). No changes needed there.

**Important:**
- Do NOT modify any content outside the LLM results table and the test metric cells
- Use consistent 2-decimal percentage format (e.g., 12.60, not 12.6)
- Ensure the table compiles correctly in LaTeX (no missing `&` or `\\`)
  </action>
  <verify>
    <automated>cd /home/turncloak/0313temp && python3 -c "
import re
with open('report/report.tex') as f:
    content = f.read()

# Check LLM table has actual experiment names
assert 'BM25' in content, 'BM25 not found in report'
assert 'Random' in content, 'Random not found in report'
assert 'Variant1' not in content, 'Template Variant1 still present'
assert 'Variant2' not in content, 'Template Variant2 still present'

# Check dev metrics are filled (not XX.XX)
# The LLM dev rows should have real numbers
assert '17.35' in content, 'BM25 k=3 F1 (17.35) not found'
assert '12.60' in content, 'Random k=0/k=1 F1 (12.60) not found'
assert '11.96' in content, 'Random k=3 F1 (11.96) not found'
assert '11.80' in content, 'Ablation F1 (11.80) not found'
assert '11.37' in content, 'Schema-only F1 (11.37) not found'

# Count remaining XX.XX -- should be exactly 6 (2 T5 test rows + 1 LLM test row = 3 rows x 2 metrics)
xx_count = len(re.findall(r'XX\\.XX', content))
print(f'Remaining XX.XX placeholders: {xx_count}')
assert xx_count == 6, f'Expected 6 XX.XX (3 test rows x 2 metrics), found {xx_count}'
print('All dev metrics filled, LLM table restructured')
"</automated>
  </verify>
  <done>LLM results table restructured with actual experiment names (BM25 k=3, Random k=0/1/3, ablations). All dev metric cells contain real values. Only 6 XX.XX remain (3 test rows x 2 metrics each), pending Gradescope.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit to Gradescope and fill test metrics</name>
  <files>report/report.tex</files>
  <action>
This task requires the user to submit prediction files to Gradescope and provide the test scores. Claude cannot access Gradescope. Once scores are provided, Claude will replace the remaining 6 XX.XX placeholders in report/report.tex (2 in the T5 test rows at lines 482-483, and 2 in the LLM test row).

Steps after receiving scores:
1. Replace XX.XX on the T5 fine-tuning test row (line 482) with the provided Query EM and Record F1
2. Replace XX.XX on the T5 from-scratch test row (line 483) with the provided Query EM and Record F1
3. Replace XX.XX on the LLM BM25 k=3 test row with the provided Query EM and Record F1

If user types "skip", leave XX.XX as-is and note in the SUMMARY that test metrics are deferred pending Gradescope submission.
  </action>
  <verify>
    <automated>cd /home/turncloak/0313temp && python3 -c "
import re
with open('report/report.tex') as f:
    content = f.read()
xx_count = len(re.findall(r'XX\\.XX', content))
print(f'Remaining XX.XX placeholders: {xx_count}')
if xx_count == 0:
    print('All placeholders filled')
else:
    print(f'{xx_count} test metric placeholders remain (deferred to Gradescope)')
"</automated>
  </verify>
  <done>Test metric placeholders either filled with Gradescope scores (0 XX.XX remaining) or explicitly deferred with user acknowledgment.</done>
</task>

</tasks>

<verification>
1. `grep -c 'Variant1' report/report.tex` returns 0 (template placeholders removed)
2. `grep -c 'BM25' report/report.tex` returns at least 1 (actual experiment names present)
3. Dev metrics 17.35, 12.60, 11.96, 11.80, 11.37 all present in report.tex
4. XX.XX count decreased from 18+ to 6 (only test rows remain) or 0 (if Gradescope scores provided)
</verification>

<success_criteria>
- LLM results table restructured with actual experiment names and dev metrics (RPT-04)
- All dev metric placeholders filled with values from experiment logs (RPT-04)
- Test metrics filled if Gradescope scores available, otherwise clearly marked (RPT-01, RPT-02, RPT-03)
- Consistent percentage format across all tables
</success_criteria>

<output>
After completion, create `.planning/phases/03-metrics-and-visualization/03-01-SUMMARY.md`
</output>
