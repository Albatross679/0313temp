---
phase: 03-metrics-and-visualization
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - script/plot_icl_sensitivity.py
  - media/icl_sensitivity.pdf
  - media/icl_sensitivity.png
  - report/report.tex
autonomous: true
requirements: [VIZ-01, VIZ-02]

must_haves:
  truths:
    - "ICL sensitivity plot exists as both PDF and PNG in media/"
    - "Plot shows Record F1 (%) on y-axis vs k (0, 1, 3) on x-axis"
    - "Random selection shown as connected line, BM25 k=3 as distinct marker"
    - "Plot is included in report.tex via includegraphics replacing the gray placeholder text"
  artifacts:
    - path: "script/plot_icl_sensitivity.py"
      provides: "Standalone script to generate the ICL sensitivity plot"
      contains: "icl_sensitivity"
    - path: "media/icl_sensitivity.pdf"
      provides: "LaTeX-compatible PDF figure for the report"
    - path: "media/icl_sensitivity.png"
      provides: "PNG version for preview"
    - path: "report/report.tex"
      provides: "Figure insertion via includegraphics"
      contains: "icl_sensitivity"
  key_links:
    - from: "script/plot_icl_sensitivity.py"
      to: "media/icl_sensitivity.pdf"
      via: "plt.savefig generates PDF output"
      pattern: "savefig.*icl_sensitivity"
    - from: "report/report.tex"
      to: "media/icl_sensitivity.pdf"
      via: "includegraphics references the PDF"
      pattern: "includegraphics.*icl_sensitivity"
---

<objective>
Create the ICL sensitivity plot (Record F1 vs k) and insert it into the report.

Purpose: The assignment requires a plot showing how Record F1 varies with the number of in-context examples k. The data points already exist from prior experiments -- this plan creates the visualization and integrates it into the LaTeX report.
Output: `media/icl_sensitivity.pdf`, `media/icl_sensitivity.png`, `script/plot_icl_sensitivity.py`, and updated `report/report.tex` with the figure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-metrics-and-visualization/03-RESEARCH.md

@report/report.tex
@experiments/llm-prompting-experiments.md

<interfaces>
<!-- ICL sensitivity data points (dev set, from experiment logs) -->

Random example selection (k-shot):
  k=0: Record F1 = 0.1260 (12.60%)
  k=1: Record F1 = 0.1260 (12.60%)
  k=3: Record F1 = 0.1196 (11.96%)

BM25 per-query example selection:
  k=3: Record F1 = 0.1735 (17.35%)

Plot location in report.tex:
  Line 517: \paragraph{ICL sensitivity to $k$:} ...
  Line 519: \textcolor{gray}{Remove this text and place your plot here!}
  -> Replace line 519 with \begin{figure}...\end{figure}

The assignment says: "The prompts and examples used for this plot should correspond
to the ones you described in Subsection \ref{sec:llm:icl}."

Reference: \subsection{In-Context Learning (ICL)}\label{sec:llm:icl} (line 308)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ICL sensitivity plot script and generate figures</name>
  <files>script/plot_icl_sensitivity.py, media/icl_sensitivity.pdf, media/icl_sensitivity.png</files>
  <action>
Create `script/plot_icl_sensitivity.py` -- a standalone script that generates the ICL sensitivity plot.

The script should:
1. Use `matplotlib.use('Agg')` as the first matplotlib call (non-interactive backend, no display needed)
2. Plot Record F1 (%) on y-axis vs k on x-axis
3. Show two series:
   - Random selection: line+markers at k=0 (12.60%), k=1 (12.60%), k=3 (11.96%) -- connected with `o-` markers
   - BM25 selection: single square marker at k=3 (17.35%) -- distinct from random line
4. Style for academic paper:
   - Figure size: `(5, 3.5)` -- compact for single-column
   - X-axis label: `Number of examples ($k$)` with fontsize 12
   - Y-axis label: `Record F1 (%)` with fontsize 12
   - X-ticks at [0, 1, 3] (the actual k values tested)
   - Legend with fontsize 10
   - Light grid (`alpha=0.3`)
   - Y-axis range: 0 to ~22% (enough headroom above max 17.35%)
   - Use distinct colors: blue (#2196F3) for random, red/orange (#FF5722) for BM25
5. Save to both `media/icl_sensitivity.pdf` (for LaTeX) and `media/icl_sensitivity.png` (for preview), both with `bbox_inches='tight'` and `dpi=300`
6. Print confirmation message with file paths

Use the code pattern from the research document (03-RESEARCH.md "ICL Sensitivity Plot" section) as the base implementation.

After creating the script, run it:
```bash
cd /home/turncloak/0313temp && python script/plot_icl_sensitivity.py
```

Verify both output files exist and are non-zero size.
  </action>
  <verify>
    <automated>cd /home/turncloak/0313temp && python script/plot_icl_sensitivity.py && python3 -c "
import os
pdf = 'media/icl_sensitivity.pdf'
png = 'media/icl_sensitivity.png'
assert os.path.exists(pdf), f'{pdf} not found'
assert os.path.getsize(pdf) > 1000, f'{pdf} too small ({os.path.getsize(pdf)} bytes)'
assert os.path.exists(png), f'{png} not found'
assert os.path.getsize(png) > 1000, f'{png} too small ({os.path.getsize(png)} bytes)'
print(f'PDF: {os.path.getsize(pdf)} bytes')
print(f'PNG: {os.path.getsize(png)} bytes')
print('Plot files verified')
"</automated>
  </verify>
  <done>ICL sensitivity plot script exists at script/plot_icl_sensitivity.py. Both media/icl_sensitivity.pdf and media/icl_sensitivity.png exist with non-trivial file sizes. Plot shows Random k=0,1,3 as connected line and BM25 k=3 as distinct point.</done>
</task>

<task type="auto">
  <name>Task 2: Insert ICL sensitivity figure into report.tex</name>
  <files>report/report.tex</files>
  <action>
Edit report/report.tex to replace the placeholder text with the actual figure.

Find line 519 (or wherever the gray placeholder text is):
```latex
\textcolor{gray}{Remove this text and place your plot here!}
```

Replace it with:
```latex
\begin{figure}[h!]
\centering
\includegraphics[width=0.7\textwidth]{media/icl_sensitivity.pdf}
\caption{ICL sensitivity to number of examples $k$ on the development set. Record F1 is shown for random example selection ($k = 0, 1, 3$) and BM25 per-query selection ($k = 3$).}
\label{fig:icl_sensitivity}
\end{figure}
```

Important:
- Use relative path `media/icl_sensitivity.pdf` (LaTeX Workshop resolves relative to report/ or project root depending on config -- the `media/` path matches the existing convention from CLAUDE.md)
- If the LaTeX compiler looks for files relative to `report/`, use `../media/icl_sensitivity.pdf` instead. Check how existing `\includegraphics` calls in the file reference images to match the convention.
- Keep the `\paragraph{ICL sensitivity to $k$:}` text above the figure -- only replace the gray placeholder line
- Do NOT modify any other part of report.tex
  </action>
  <verify>
    <automated>cd /home/turncloak/0313temp && python3 -c "
with open('report/report.tex') as f:
    content = f.read()

# Check placeholder removed
assert 'Remove this text and place your plot here' not in content, 'Placeholder text still present'

# Check figure inserted
assert 'icl_sensitivity' in content, 'icl_sensitivity not found in report'
assert 'includegraphics' in content and 'icl_sensitivity' in content, 'includegraphics for icl_sensitivity not found'
assert 'fig:icl_sensitivity' in content, 'Figure label not found'

print('Figure inserted into report.tex')
"</automated>
  </verify>
  <done>The gray placeholder text is replaced with a proper LaTeX figure environment referencing media/icl_sensitivity.pdf. The figure has a caption describing the ICL sensitivity experiment and a label for cross-referencing.</done>
</task>

</tasks>

<verification>
1. `test -f media/icl_sensitivity.pdf` passes (VIZ-01)
2. `test -f media/icl_sensitivity.png` passes (VIZ-01)
3. `grep 'icl_sensitivity' report/report.tex` returns matches (VIZ-02)
4. `grep 'Remove this text' report/report.tex` returns no matches (placeholder removed)
5. `python script/plot_icl_sensitivity.py` runs without errors and regenerates the plot
</verification>

<success_criteria>
- ICL sensitivity plot exists as PDF and PNG in media/ (VIZ-01)
- Plot shows Record F1 vs k with random (k=0,1,3) and BM25 (k=3) series (VIZ-01)
- Plot is inserted into report.tex via \includegraphics (VIZ-02)
- Placeholder text removed from report.tex (VIZ-02)
- Plot script is reproducible (script/plot_icl_sensitivity.py) (VIZ-01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-metrics-and-visualization/03-02-SUMMARY.md`
</output>
